/**
 * NPM serve ad importare i paccchetti gia creati da altri 
 * 
 * pacchetti  dipendenza e moduli   sono la stessa cosa
 * package dependency module
 * 
 * non c'e  controllo sulla pubblicazione  quindi attenzioe a cosa si mette in casa!
 *  controllare dal sito npmjs.com  almeno il numero dei download
 * per capire la vallidita del pacchetto 
 * 
 * npm
 * npm -v
 * npm install nome_pacchetto           // in locale nella cartella del progetto
 * npm -g install nome_pacchetto        // in globale sul pc sempre disponibile per tutti i progetti
 * npm unistall nome_pacchetto
 */

/**
 * il Package JSON
 *  si tratta del file che contiene tutte le info piu rilevanti per il nostro progetto
 * 
 * npm init   crea il contenuto di base del progetto in automatico
 * 
 * con  npm init -y  crea il file ma lo lascia vuoto
 * 
 * 
 * 
 * npm init lodash
 * viene installato il pacchetto e modificato il package.json di conseguenza
 * si installa in nome_modules
 * e crea il file  package-lock.json
 * 
 *  
 */